Face Detection Attendance System
Pandas Basics, Features and Labels
The objective of this week's task is to learn the basics of Python Pandas and understand how to load, inspect, and clean a dataset.
- Created an attendance dataset (`attendance.csv`)
- Loaded the dataset using Pandas
- Displayed the first five rows using `head()`
- Checked dataset information using `info()`
- Identified missing values using `isnull().sum()`
- Removed duplicate rows using `drop_duplicates()`
- Filled missing attendance values using `fillna()`
- Saved the cleaned dataset as `cleaned_attendance.csv`
- Python Programming Basics
- Pandas Library
- Working with CSV Files
- Data Cleaning
- Handling Missing Values
- Removing Duplicate Records
- Understanding Features and Labels
- Visual Studio Code
- GitHub Basics
Successfully loaded, cleaned, and saved the attendance dataset using Python and Pandas as part of Week 1 of the EDP AI/ML Internship.
